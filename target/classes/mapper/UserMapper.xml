<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.roy.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.roy.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="realName" property="realName"/>
        <result column="status" property="status"/>
        <result column="role" property="role"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectUserByUsername" resultMap="BaseResultMap">
        SELECT *
        FROM user
        WHERE username = #{username}
    </select>

    <select id="selectUserInfoById" resultType="com.roy.entity.User">
        SELECT 
            u.username,
            u.realname,
            u.role,
            u.email,
            u.phone,
            u.status
        FROM user u
        WHERE u.id = #{id}
    </select>

    <!-- 其他现有方法保持不变 -->
    <insert id="insert" parameterType="com.roy.entity.User">
        INSERT INTO user (username, password, role)
        VALUES (#{username}, #{password}, #{role})
    </insert>

    <select id="selectUserByUsernameAndRole" resultMap="BaseResultMap">
        SELECT id, username, password, role
        FROM user
        WHERE username = #{username}
          AND role = #{role}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT id, username, password, role
        FROM user
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id,
               username,
               password,
               role,
               realName,
               status,
               email,
               phone,
               create_time,
               update_time
        FROM user
        WHERE id = #{id}
    </select>

    <select id="selectOne" resultMap="BaseResultMap">
        SELECT id, username, realname AS realName, role, status
        FROM user
        WHERE id = #{id}
    </select>

    <update id="update">
        UPDATE user
        SET ${ew.sqlSet}=
                ${ew.customSqlSegment}
    </update>

    <update id="updateById" parameterType="com.roy.entity.User">
        UPDATE user
        SET username    = #{user.username},
            password    = #{user.password},
            realName    = #{user.realName},
            role        = #{user.role},
            status      = #{user.status},
            update_time = NOW()
        WHERE id = #{user.id}
    </update>

    <delete id="deleteById">
        DELETE
        FROM user
        WHERE id = #{id}
    </delete>

    <select id="countRegisteredStudents" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM user
        WHERE role = 'STUDENT'
    </select>

    <select id="countRegisteredTeachers" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM user
        WHERE role = 'TEACHER'
    </select>

    <select id="selectCount" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM user ${ew.customSqlSegment}
    </select>

    <select id="getUsersInfo" resultType="com.roy.entity.User">
        SELECT id,
               username,
               realname    AS realName,
               role,
               email,
               phone,
               status,
               create_time AS createTime
        FROM user;
    </select>

    <update id="updateUserStatus" parameterType="com.roy.entity.User">
        UPDATE user
        SET status = #{status}
        WHERE id = #{id}
    </update>

    <select id="selectUserByDWays" resultType="com.roy.entity.User">
        SELECT id, username, realname AS realName, role, status, create_time AS createTime
        FROM user
        <where>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="role != null and role != ''">
                AND role = #{role}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <insert id="insertNewUser" parameterType="com.roy.entity.User">
        INSERT INTO user (username, password, role, create_time, update_time)
        VALUES (#{username}, #{password}, #{role}, NOW(), NOW())
    </insert>

    <update id="updateSelfInfo" parameterType="com.roy.entity.User">
        UPDATE user
        SET username = #{username},
            realname = #{realName},
            email = #{email},
            phone = #{phone},
            update_time = NOW()
        WHERE id = #{id}
    </update>
</mapper>
